<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@android:color/white">

    <TextView
        android:id="@+id/bloodGroupTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Blood Group"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:layout_gravity="center_horizontal"
        android:paddingBottom="16dp" />

    <GridLayout
        android:id="@+id/bloodGroupGrid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:rowCount="4"
        android:alignmentMode="alignMargins"
        android:rowOrderPreserved="false"
        android:columnOrderPreserved="false"
        android:useDefaultMargins="true">

        <!-- Each blood group -->
        <Button android:text="A+" style="@style/BloodGroupButton"/>
        <Button android:text="A-" style="@style/BloodGroupButton"/>
        <Button android:text="B+" style="@style/BloodGroupButton"/>
        <Button android:text="B-" style="@style/BloodGroupButton"/>
        <Button android:text="AB+" style="@style/BloodGroupButton"/>
        <Button android:text="AB-" style="@style/BloodGroupButton"/>
        <Button android:text="O+" style="@style/BloodGroupButton"/>
        <Button android:text="O-" style="@style/BloodGroupButton"/>

    </GridLayout>
</LinearLayout>
